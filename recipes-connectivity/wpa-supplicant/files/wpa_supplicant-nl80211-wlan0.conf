ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=0
update_config=1
country=FI

# remove sca_ssid=1 if your SSID is not hidden
# set ssid to your wifi network name
# set psk to your wifi password
network={
	scan_ssid=1
        ssid=""
        psk=""
}

# note: Raspberry Pi Zero W only supports 2.4GHz wifi
